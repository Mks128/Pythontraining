def is_palindrome(s):
    s = s.replace(" ", "").lower()
    length = len(s)
    for i in range(length // 2):
        if s[i] != s[length - 1 - i]:
            return False                 
    return True
        
result = is_palindrome("Madam") 
print(result)       
    # Remove spaces and convert to lowercase
    
